{
  "name": "domi-backup",
  "module": "src/run.ts",
  "type": "module",
  "scripts": {
    "dev": "bun run src/run.ts",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/run.ts --compile --target=bun-linux-x64 --outfile=bin/domi-backup"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "yaml": "^2.8.3"
  }
}
